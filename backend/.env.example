# PilotOS Backend Environment Variables

# Database
DATABASE_URL="postgresql://postgres:password@localhost:5432/pilotos?schema=public"

# Server
PORT=3001
NODE_ENV=development

# JWT Secret (para magic link)
JWT_SECRET=pilotos-secret-change-in-production

# MinIO Storage
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=pilotos-fotos

# WhatsApp (Meta Business API)
WHATSAPP_TOKEN=your-whatsapp-token
WHATSAPP_PHONE_NUMBER_ID=your-phone-number-id
